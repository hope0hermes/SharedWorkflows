# Reusable Release Workflow
#
# This workflow automates version bumping and release creation.
# It analyzes commit messages, bumps the version, updates CHANGELOG, and creates a PR.
#
# Usage:
#   jobs:
#     release:
#       uses: hope0hermes/SharedWorkflows/.github/workflows/reusable-release.yml
#       with:
#         package-path: "src/my_package/__init__.py"
#       secrets:
#         PAT_TOKEN: ${{ secrets.PAT_TOKEN }}

name: Reusable Release

on:
  workflow_call:
    inputs:
      package-path:
        description: 'Path to package __init__.py (e.g., src/strava_analyzer/__init__.py)'
        required: true
        type: string
    
    secrets:
      PAT_TOKEN:
        description: 'Personal Access Token with repo and pull_request scopes'
        required: true

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.PAT_TOKEN }}
      
      - name: Checkout SharedWorkflows actions
        uses: actions/checkout@v4
        with:
          repository: hope0hermes/SharedWorkflows
          ref: main
          path: .github/actions-temp
      
      - name: Check skip conditions
        id: skip_check
        uses: .github/actions-temp/actions/check-skip-conditions
      
      - name: Determine version bump type
        if: steps.skip_check.outputs.should_skip != 'true'
        id: bump_type
        uses: .github/actions-temp/actions/determine-version-bump
      
      - name: Bump version
        if: steps.skip_check.outputs.should_skip != 'true' && steps.bump_type.outputs.bump_type != 'none'
        id: version
        uses: .github/actions-temp/actions/bump-version
        with:
          bump-type: ${{ steps.bump_type.outputs.bump_type }}
      
      - name: Update changelog
        if: steps.skip_check.outputs.should_skip != 'true' && steps.bump_type.outputs.bump_type != 'none'
        uses: .github/actions-temp/actions/update-changelog
        with:
          version: ${{ steps.version.outputs.new_version }}
          commits: ${{ steps.bump_type.outputs.commits }}
      
      - name: Create version bump PR
        if: steps.skip_check.outputs.should_skip != 'true' && steps.bump_type.outputs.bump_type != 'none'
        uses: .github/actions-temp/actions/create-version-pr
        with:
          version: ${{ steps.version.outputs.new_version }}
          package-path: ${{ inputs.package-path }}
          github-token: ${{ secrets.PAT_TOKEN }}
